# python Beautifier / Unpacker

*Clean your python files with style*


## What this thing can do:

- Unpack base64 packed python scripts (ex: exec(base64.b64decode('ZXhlYy') => print('test')
- Beautify, format python scripts
